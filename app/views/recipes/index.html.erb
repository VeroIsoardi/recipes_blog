<% content_for :title, "Recetas" %>

<div class="relative z-10">
  <div class="flex items-start justify-between mb-4 md:mb-12 header">
    <div class="title text-start">
      <%= phosphor_icon('newspaper', class: 'hidden md:block text-cyan-700', width: '64', height: '64')%>
      <h1 class="text-gray-900 font-bold text-2xl md:text-5xl">Curaduría de recetas por <span class="text-fuchsia-700">Vero</span></h1>
    </div>

    <% if authenticated? %>
      <%= link_to "Nueva receta", new_recipe_path, class: "mt-6 rounded-lg py-1 px-2 bg-fuchsia-800 text-white text-sm" %>
    <% end %>
  </div>

  <div class="flex justify-center w-full">
    <%= form_with model: false, url: recipes_path, method: :get, data: { turbo_action: "replace", turbo_permanent: "", controller: 'filter' }, class: 'flex gap-3 w-1/2' do |f| %>
      <%= f.search_field :query,
        placeholder: 'Buscar por nombre o categoría',
        autocomplete: 'off',
        value: params[:query],
        data: { filter_target: 'input', action: 'filter#submit' },
        class: 'w-full md:!w-[400px] rounded-lg flex-1 appearance-none border border-gray-400 py-2 px-4 bg-white text-gray-700 placeholder-gray-400 shadow-sm text-base focus:outline-none focus:ring-2 focus:ring-fuchsia-600 focus:border-transparent' %>
      <%= f.submit "Buscar", class: "flex-shrink-0 px-4 py-2 text-base font-semibold text-white bg-fuchsia-600 rounded-lg shadow-md hover:bg-fuchsia-700 focus:outline-none focus:ring-2 focus:ring-fuchsia-500 focus:ring-offset-2 focus:ring-offset-purple-200" %>
    <% end %>
  </div>

  <% if @recipes.present? %>
    <div class="grid grid-cols-1 mt-4 md:mt-16" id="recipes">
      <%= render @recipes %>
    </div>

    <% if @pagy.next.present? %>
      <%= turbo_frame_tag :next_page, src: recipes_path(page: @pagy.next, query: params[:query], format: :turbo_stream), loading: :lazy, class: 'flex justify-center' do %>
        <div class="text-center">
          <div role="status">
            <%= phosphor_icon('spinner-gap', class: 'inline w-8 h-8 text-gray-200 animate-spin fill-fuchsia-600')%>
          </div>
          <span class="sr-only">Loading...</span>
        </div>
      <% end %>
    <% end %>
  <% else %>
    <div class="w-full flex items-center mt-4 md:mt-16">
      <div class="w-3/5">
        <h2 class="text-2xl font-semibold">Ups! No encontramos nada</h2>
        <p class="text-md mt-2 text-wrap">
          El Monstruo de las Galletas pasó por aquí y se llevó todos los resultados... ¡pero no te preocupes! Prueba con otra búsqueda o echa un vistazo a nuestras recetas destacadas. Seguro que encuentras algo para devorar (¡prometemos tener más que solo galletas! 😉).
        </p>
      </div>
      <%= image_tag 'cookie_monster.png', size: '30%' %>
    </div>
  <% end %>
</div>
