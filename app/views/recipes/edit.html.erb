<%= content_for(:back_button_url, recipes_path) %>

<div class="card w-full">
  <h1 class="font-bold text-4xl">Editar receta</h1>

  <%= render "form", recipe: @recipe %>

  <%= link_to "Volver", recipes_path, class: "ml-2 btn-secondary inline-block" %>
</div>
